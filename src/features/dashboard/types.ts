// Re-exported at the feature boundary so dashboard code imports from its own
// feature folder rather than reaching into `@/lib/api/types` directly.
export type { DashboardSalesOut, SalesOrderListItem } from '@/lib/api/types';
