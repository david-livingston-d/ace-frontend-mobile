# Canvas edits — 2026-08-28 (David, Claude Design project `ac15968c…`)

Source of truth for these deltas: `mockup-canvas-2026-08-28.dc.html` (the "Workholic Mobile App" canvas as edited by David), diffed against the 2026-08-26 snapshot. They **override** the spec's D2/D3 and the `redesign/` preview wherever they differ. Tasks 5–8 implement them.

| # | Where | Change | RN implication |
|---|---|---|---|
| 1 | Timeline (D4 artboard, also E2) | Every node gets a 22 px circular **icon disc** on the rail (`.tli`: card bg, hairline ring, `0 4px 10px rgba(13,13,13,.14)` shadow; icon 12 px): created = plus, ready for stock check = check, stock check = search, reserved = lock, delivery = truck, payment = receipt, return = return-arrow, approval = pen, closed = check. Current node = halo `0 0 0 2px page, 0 0 0 4px rgba(jet,.3)`; future nodes at 45 % opacity. Rail moves to `left 10px`, content `padding-left 34`. Dark: disc `#26262a`, ring `rgba(244,244,244,.14)`, rail `#2e2e32`. The old plain dot is removed. | `TimelineList`: new `TimelineNode` with `IconDisc size=22` + lucide icon by event kind; rail 2 px; current = pulsing halo; future = 0.45 opacity. |
| 2 | Home hero tile | The big number gets `text-shadow: 0 3px 8px rgba(0,0,0,.55)` (raised digit on the glossy tile). | `HeroTile` value text: `textShadowColor rgba(0,0,0,.55)`, `textShadowOffset {0,3}`, `textShadowRadius 8`. |
| 3 | Home due strip | Three small cards → three **flex-1 chips** in one row: bold count (13/700) + label; Overdue chip = red text + `0 10px 22px rgba(168,60,49,.2)` + inset ring `rgba(168,60,49,.3)`; Due today = amber equivalents (`rgba(150,103,28,.18)` / `.3`); This week = neutral chip. Caption under the row: "Tap a tag to filter the order list" (11 muted, centred). | `DueStrip`: `Chip` with `count` slot + tone shadow/ring props; caption `Text caption` centred. |
| 4 | Variant picker rows (C1/C3) | The size badge on each stepper row becomes `min-width 52, padding 0 8, height 38, radius 12, font 11/600 letter-spaced` so long size labels fit. | `VariantRow` size badge: minWidth 52, paddingH 8, h 38, r 12, `badge`-style text. |
| 5 | Variant picker — stock (C2 renamed "Stock warning — **no stock info shown; system flags on add**") | Rows show **no stock hint at rest**. When the entered quantity exceeds available, the warning appears on add (note card: "N units of SKU exceed the M available in Main Warehouse. The order can still be placed — a shortage is raised for Production."). | `VariantRow` drops `StockHint` at rest; `VariantPickerSheet` shows the exceed warning `Banner tone="warning"` when qty > available (data from the variant payload), non-blocking; footer stays enabled. Spec D3 "StockHint always visible" is superseded. |
| 6 | Success screen (C6) | Under the primary, **View order** and **Dashboard** (home icon) sit side by side as two outline buttons. | `SuccessScreen`: row of two `Button variant="outline"` (View order / Dashboard) + Record payment (permission) + New order ghost; hardware back still → Orders (Task 1). |
| 7 | Order detail (D2/F3) | Payments chips wrap (`gap 6`, `padding 4×9`), "Outstanding" abbreviated to **"Outst."** when in the chip row; the open-order action bar is one row of three no-wrap buttons — primary `flex 1.5` (Record delivery), outline `flex 1` (Payment), outline `flex 1` (third action; for us **Invoice** when permitted, else PDF). | `PaymentsSection` chips `StatusChip size="sm"` wrapping; `ActionBar` layout: primary 1.5 + two outline 1 + overflow into a second row only when >3 actions. |
| 8 | Sales Head 7-day chart (F1) | The "Today" bar was accent yellow → now a **white card bar** with `0 6px 14px rgba(13,13,13,.18)` shadow + hairline ring (no accent colour, as ruled). | `SevenDayChart` today bar = `card` surface + `shadow('raised')` + ring; other bars `dim`. |

Everything else on the canvas is unchanged from the 2026-08-26 snapshot; the `redesign/` preview remains the screen-by-screen reference and its CSS the token source, with the eight rows above layered on top.
